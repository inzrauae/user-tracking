╔══════════════════════════════════════════════════════════════════╗
║                    QUICK REFERENCE CARD                          ║
╚══════════════════════════════════════════════════════════════════╝

📁 PROJECT STRUCTURE
═══════════════════════════════════════════════════════════════════
user-tracking/
├── server/                  ← BACKEND (Node.js + Express + MongoDB)
│   ├── models/             ← Database schemas (4 models)
│   ├── routes/             ← API endpoints (6 route files)
│   ├── middleware/         ← Auth middleware
│   ├── index.js           ← Server entry point
│   ├── seed.js            ← Database seeder
│   └── package.json       ← Backend dependencies
├── services/
│   └── api.ts             ← Frontend API client (ready to use)
├── components/            ← React UI components
├── App.tsx               ← Main React app
└── types.ts              ← TypeScript definitions

🚀 QUICK START COMMANDS
═══════════════════════════════════════════════════════════════════
# 1. Install everything
npm install
cd server && npm install && cd ..

# 2. Start MongoDB  
net start MongoDB

# 3. Seed demo data (optional)
cd server && npm run seed && cd ..

# 4. Run application
.\start.ps1

# Or manually:
# Terminal 1: cd server && npm run dev
# Terminal 2: npm run dev

🌐 ACCESS POINTS
═══════════════════════════════════════════════════════════════════
Frontend:    http://localhost:3000
Backend:     http://localhost:5000/api
Health:      http://localhost:5000/api/health

🔑 DEMO CREDENTIALS
═══════════════════════════════════════════════════════════════════
Email                  Password    Role
─────────────────────────────────────────────────────────────────
admin@demo.com         demo123     ADMIN
employee@demo.com      demo123     EMPLOYEE
mike@demo.com          demo123     TEAM_LEADER
sarah@demo.com         demo123     EMPLOYEE

Or use: "Login as Admin" / "Login as Employee" buttons

🔌 API ENDPOINTS
═══════════════════════════════════════════════════════════════════
Authentication
  POST   /api/auth/register          Register new user
  POST   /api/auth/login             Login
  POST   /api/auth/demo-login        Demo login
  POST   /api/auth/logout            Logout

Users
  GET    /api/users                  Get all users
  GET    /api/users/:id              Get user by ID
  PUT    /api/users/:id              Update user
  DELETE /api/users/:id              Delete user

Tasks
  GET    /api/tasks                  Get tasks (+ filters)
  POST   /api/tasks                  Create task
  PUT    /api/tasks/:id              Update task
  DELETE /api/tasks/:id              Delete task

Time Tracking
  POST   /api/time-entries/start     Start tracking
  POST   /api/time-entries/stop/:id  Stop tracking
  PUT    /api/time-entries/:id       Update activity
  GET    /api/time-entries/summary/:userId

Screenshots
  GET    /api/screenshots            Get all
  POST   /api/screenshots            Create
  GET    /api/screenshots/user/:id   Get user screenshots

Statistics
  GET    /api/stats/dashboard/:id    Dashboard stats
  GET    /api/stats/weekly/:id       Weekly data
  GET    /api/stats/team             Team overview

📝 TESTING COMMANDS
═══════════════════════════════════════════════════════════════════
# Health check
curl http://localhost:5000/api/health

# Demo login (PowerShell)
$body = @{role="ADMIN"} | ConvertTo-Json
Invoke-RestMethod -Uri "http://localhost:5000/api/auth/demo-login" `
  -Method POST -Body $body -ContentType "application/json"

⚙️ CONFIGURATION FILES
═══════════════════════════════════════════════════════════════════
.env (Frontend)
  VITE_API_URL=http://localhost:5000/api

server/.env (Backend)
  PORT=5000
  MONGODB_URI=mongodb://localhost:27017/user-tracking
  JWT_SECRET=your-secret-key-here
  NODE_ENV=development

🛠️ USEFUL COMMANDS
═══════════════════════════════════════════════════════════════════
# Seed database
cd server && npm run seed

# Clear and reseed
cd server
npm run seed

# Check MongoDB status
Get-Service MongoDB

# Start MongoDB
net start MongoDB

# Stop MongoDB
net stop MongoDB

# Backend only
cd server && npm run dev

# Frontend only
npm run dev

# Build for production
npm run build

📚 DOCUMENTATION
═══════════════════════════════════════════════════════════════════
SETUP.md                  → Installation guide
BACKEND_README.md         → Complete API docs
IMPLEMENTATION_SUMMARY.md → What was built
README_NEW.md            → Project overview

🐛 TROUBLESHOOTING
═══════════════════════════════════════════════════════════════════
Problem: MongoDB won't start
Fix: net start MongoDB
     OR: mongod --dbpath C:\data\db

Problem: Port already in use
Fix: Change PORT in server/.env

Problem: CORS errors
Fix: Check FRONTEND_URL in server/.env

Problem: Buttons not working
Fix: Clear browser cache, reload page

Problem: Module not found
Fix: npm install (or cd server && npm install)

✨ FEATURES IMPLEMENTED
═══════════════════════════════════════════════════════════════════
✅ JWT Authentication       ✅ Time Tracking
✅ User Management          ✅ Activity Monitoring
✅ Task Management          ✅ Idle Detection
✅ Screenshot Tracking      ✅ Team Dashboard
✅ Weekly Analytics         ✅ Role-based Access
✅ Database Seeding         ✅ API Documentation

═══════════════════════════════════════════════════════════════════
                 Ready for Development! 🚀
═══════════════════════════════════════════════════════════════════
